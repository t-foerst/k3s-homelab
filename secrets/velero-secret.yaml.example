apiVersion: v1
kind: Secret
metadata:
  name: velero-secret
  namespace: velero
type: Opaque
stringData:
  cloud: |
    [default]
    aws_access_key_id=<GARAGE_DEFAULT_ACCESS_KEY>
    aws_secret_access_key=<GARAGE_DEFAULT_SECRET_KEY>
